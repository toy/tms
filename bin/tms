#!/usr/bin/env ruby

begin
  require 'tms'
rescue LoadError
  require 'rubygems'
  require 'tms'
end

case ARGV.length
when 0
  Tms.list
when 1
  id = ARGV[0].to_i
  Tms.diff(id - 1, id)
when 2
  Tms.diff(ARGV[0].to_i, ARGV[1].to_i)
else
  abort 'Max two arguments'
end
